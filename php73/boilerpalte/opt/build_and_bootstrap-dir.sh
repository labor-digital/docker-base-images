#!/bin/bash
# ORDER: 1 | WHERE: PROD (Build) and DEV (Container Start)
# Allows you to create additional required directories and set their permissions
# You can use set_initial_directory_permissions() and ensure_directory() (See .bashrc) to make your life easier.